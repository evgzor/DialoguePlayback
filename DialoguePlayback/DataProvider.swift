//
//  DataProvider.swift
//  DialoguePlayback
//
//  Created by Eugene Zorin on 13.09.2020.
//  Copyright © 2020 Eugene Zorin. All rights reserved.
//

import Foundation
typealias DataProvider = [PurpleDataProvider]

struct PurpleDataProvider: Codable {
    let line: String
}

// MARK: Convenience initializers

extension PurpleDataProvider {
    init?(data: Data) {
        guard let me = try? JSONDecoder().decode(PurpleDataProvider.self, from: data) else { return nil }
        self = me
    }

    init?(_ json: String, using encoding: String.Encoding = .utf8) {
        guard let data = json.data(using: encoding) else { return nil }
        self.init(data: data)
    }

    init?(fromURL url: String) {
        guard let url = URL(string: url) else { return nil }
        guard let data = try? Data(contentsOf: url) else { return nil }
        self.init(data: data)
    }

    var jsonData: Data? {
        return try? JSONEncoder().encode(self)
    }

    var json: String? {
        guard let data = self.jsonData else { return nil }
        return String(data: data, encoding: .utf8)
    }
}

extension Array where Element == DataProvider.Element {
    init?(data: Data) {
        guard let me = try? JSONDecoder().decode(DataProvider.self, from: data) else { return nil }
        self = me
    }

    init?(_ json: String, using encoding: String.Encoding = .utf8) {
        guard let data = json.data(using: encoding) else { return nil }
        self.init(data: data)
    }

    init?(fromURL url: String) {
        guard let url = URL(string: url) else { return nil }
        guard let data = try? Data(contentsOf: url) else { return nil }
        self.init(data: data)
    }

    var jsonData: Data? {
        return try? JSONEncoder().encode(self)
    }

    var json: String? {
        guard let data = self.jsonData else { return nil }
        return String(data: data, encoding: .utf8)
    }
}
